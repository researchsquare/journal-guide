FROM node:18-alpine

WORKDIR /app

# Copy package.json and lock file
COPY package.json package-lock.json* ./

# Install all dependencies
RUN npm install

# Copy project files (still needed for initial container build)
COPY . .

# Run in dev mode (hot reload)
CMD ["npm", "run", "dev"]
